class Solution {
    public int maxProfit(int[] prices) {
    int s= 0;
    int h= Integer.MIN_VALUE;
    for (int price : prices) {
      s= Math.max(s, h+ price);
      h= Math.max(h, s- price);
    }
    return s;
    }
}
